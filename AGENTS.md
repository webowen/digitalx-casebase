# DigitalX AI 开发规则

本文件用于约束 Codex、Cursor 或其他 Coding Agent。开始开发前，先读 `README.md` 与 `docs/` 中和本次任务相关的文件。案例内容、AI解析和阅读器修改必须同时遵守 `docs/09-DigitalX案例内容与编辑规范V1.0.md`。

## 开发顺序

1. 先说明需求理解、影响范围和验收条件。
2. 对跨页面、数据模型或架构修改，先给 Plan，再开始改代码。
3. 一次只完成一个可独立验收的功能，避免把多个大功能混在一个提交中。
4. 修改完成后运行与改动匹配的类型检查、Lint、构建和测试。
5. 同步更新相关文档及 `CHANGELOG.md`。

## 产品边界

- 不得把模拟交互描述为已接入的真实能力。
- 不得擅自改变“导入资料 → 解析 → 人工复核 → 位置确认 → 发布入库”的核心流程。
- 新增功能必须说明用户、入口、数据来源、数据去向、权限和失败处理。
- 发布到前台的案例必须能追溯来源、证据等级和人工复核结果。
- 已核实事实、来源方声明、AI推断、编辑判断和未决事项必须在数据层区分。
- AI解析任务必须保留阶段状态；联网失败只能降级为草稿，不得伪装为完整研究。
- 阅读模式与研读模式应读取同一案例实体，不维护两份互相漂移的数据。

## 代码与数据

- 不把密钥、口令、Token、真实个人信息写入源码、示例数据或 Git。
- 环境变量只在 `.env.example` 声明名称，真实值写入 `.env.local` 或部署平台。
- 不直接操作或删除生产数据；数据库结构变化必须使用可审查的 migration。
- 不在 `main` 分支上直接开发较大功能；使用功能分支和 Pull Request。
- 不推倒重写当前 V1；应在保持界面和业务可用的前提下逐步拆分大文件。
- 不提交 `node_modules`、`dist`、`.next`、`.sites-runtime`、`.wrangler`、`.env.local`。

## 设计规则

- 遵循 `docs/04-设计规范.md`。
- 公开站、管理端和研读界面统一采用克制的 Mintlify 式设计纪律。
- 阅读器保留微信读书式连续阅读体验，不把正文切割成大量后台卡片。
- 白底、黑字、薄边框为主；薄荷绿只用于关键动作、选中和成功状态。
- 卡片默认 12px 圆角，输入框 8px，主要按钮为胶囊形。
- 保持 Inter 与 Geist Mono 字体分工，不随意引入第三套字体。
- 新页面必须检查桌面端与手机端，不得产生横向溢出。

## 最低验收

涉及界面或业务修改时，至少确认：

- `/` 可打开，搜索和筛选没有回归；
- `/admin` 可打开，当前演示流程仍可运行；
- 一个案例详情可打开，阅读/研读切换正常；
- TypeScript 检查通过；
- Lint 通过；
- 构建与适用测试通过；
- 文档没有把未实现能力写成已实现。
