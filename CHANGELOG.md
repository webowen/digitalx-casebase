# 版本记录

本文档只记录对用户、业务流程、数据模型、部署方式或开发交接有意义的变化。

## [1.4.0] - 2026-07-29

### 正式发布收口

- V1.4 地图案例工作台通过验收并成为网站根路由 `/` 的默认公开入口。
- `/workbench` 继续作为兼容入口，避免既有分享链接失效。
- V1.3 经典首页迁移至 `/legacy`，停止功能开发，仅用于历史回看和版本对比。
- 管理端、AI解析、案例阅读器、图片证据和现有案例数据模型保持不变。
- 更新产品版本、README、路由说明与自动测试，形成可回滚的 V1.4 正式基线。

### 品牌视觉

- 左上角替换为用户提供的 `Digital + Innovate` 圆形标识，并保留清晰的品牌名称与版本信息。
- 从标识提取深蓝、湖蓝、青色与薄荷色作为工作台主题，统一搜索框、选中状态、主按钮、案例预览、统计卡片和地图标记。
- 渐变只用于品牌重点和关键操作，侧栏与地图背景保持低饱和浅色，确保长时间浏览与文字对比度。
- 品牌样式限定在 V1.4 地图工作台，不改变 V1.3 首页、管理端和案例阅读器的既有视觉。

### 稳定性修复

- 修复旧版或不完整本地案例坐标异常时可能导致地图工作台崩溃的问题。
- 稳定地图选择回调，避免筛选和目录状态变化时重复重建高德聚合图层。
- 增加地图初始化、点位聚合与视野调整的错误兜底和定时器清理。
- 保留无有效坐标案例的目录与阅读入口，仅从地图点位中安全排除。
- 按高德 MarkerCluster 官方回调结构绘制标记，不再读取回调中不存在的 `data` 字段。

### 阶段一

- 新增独立 `/workbench` 路由，在不替换旧首页的前提下启动渐进式改造。
- 建立全屏地图、顶部全局搜索、左侧多维目录、中央案例预览和右侧筛选分析布局。
- 左侧目录支持按分类、地区和专题组织，点击案例后与地图城市和中央预览同步。
- 复用现有公开案例、本地已发布案例、高德地图和完整阅读器，不复制第二套案例实体。
- 地图组件增加自适应高度参数，旧首页继续使用原有高度。
- 补齐 V1.4 产品需求、页面交互、验收标准、技术方案和分阶段开发计划。

## [1.3.0] - 2026-07-29

### 案例文章阅读器

- 重构案例封面、项目元数据、章节导语、关键结论、正文、图片证据、来源与编辑说明的阅读层级。
- 根据实际正文长度估算阅读时间；关键结论改为卡片式摘要，普通章节保留连续正文与编号要点。
- 桌面端采用双页书册，平板与手机采用单页阅读；阅读主题、字号、目录和阅读进度继续保存在当前设备。

### 真实翻页交互

- 翻页模式升级为带正反纸面、书脊、页边、透视旋转和动态阴影的 3D 翻书动画，不再只是横向滚动。
- 支持左右翻页按钮、键盘方向键、手机左右滑动和屏幕边缘点击。
- 尊重系统“减少动态效果”设置，在需要时自动降级为无动画换页；上下滚动模式继续保留。

### 工程稳定性

- 修复 Cloudflare D1 预留绑定的 TypeScript 类型收窄，完整类型检查恢复通过。
- 案例图片统一使用 Next Image 组件的非优化模式，兼容站点对象存储地址并消除图片规范警告。
- 更新过期的管理端渲染测试，新增沉浸翻书、非法图片路径和空图片上传回归测试。

## [1.2.0-media-alpha.1] - 2026-07-28

### 图片证据系统

- 文本型 PDF 在浏览器内同时提取正文和高信息密度页面，优先保留平台界面、驾驶舱、架构图、地图和现场照片候选。
- 网页导入会尝试读取 `og:image` 与正文图片；遇到微信等反爬限制时仍可继续使用粘贴正文或改用 PDF。
- AI根据页码、附近文字和视觉密度，从候选中选择最多6张，生成图片类型、目标章节、图注、无障碍说明和置信度。
- 管理端新增图片证据复核，可查看原图、修改类型与章节、编辑图注、取消收录并完成人工确认；未确认图片不能随案例发布。
- 入选原图保存至 Sites R2，案例正文只保存图片元数据；删除本地案例时同步清理对应图片对象。
- 阅读与研读模式会在对应业务章节中展示已确认图片，并标注原始 PDF 页码或网页来源。

## [1.2.0-editorial-alpha.1] - 2026-07-28

### 项目身份核验

- 将原始资料标题与正式项目名称拆分为两个字段，AI必须优先核验具体项目、平台或系统全称。
- 新增候选名称、别名、名称置信度、歧义说明和名称核验证据；证据不足时必须进入人工确认。
- 联网检索第一组查询专门用于项目身份识别，发布前要求正式名称通过人工确认。

### 结构化案例文章

- 用固定业务章节的数据模型替代无序的 Markdown 长文，统一导读、关键结论、正文段落、要点和证据引用。
- AI按项目概况、建设背景、目标、架构、功能、实施运营、投资采购、实际成效、问题边界、经验与时间线组织内容；无证据章节自动省略。
- 管理端新增章节化复核编辑器，并在发布前检查章节数量、正文长度和重复章节。
- 阅读模式与研读模式优先使用新文章结构；既有案例的旧版研究报告继续兼容显示。

## [1.1.0-ai-parser-alpha.10] - 2026-07-28

### 扫描件容错

- 扫描 PDF 的 Gemini 视觉识别遇到高负载、服务繁忙或临时连接失败时自动重试。
- 主模型连续失败后自动切换 `gemini-3.5-flash-lite` 备用模型；成功结果会记录实际模型与降级原因。
- 多模型均失败时返回明确中文提示，不再直接向用户显示上游英文错误。

## [1.1.0-ai-parser-alpha.9] - 2026-07-28

### 容错

- DeepSeek返回临时高负载、服务繁忙或连接失败时，按短间隔自动重试最多2次。
- 多次重试后仍失败时，返回明确的中文提示，并说明故障来自DeepSeek而不是Tavily联网搜索。
- 基础结构化解析和研究报告二次扩写共用相同的重试与错误分类策略。

## [1.1.0-ai-parser-alpha.8] - 2026-07-28

### 优化

- 真实联网测试确认 Tavily 可执行2次搜索并返回可追溯来源。
- 当联网研究报告少于3000字时，自动触发一次只针对研究报告的 DeepSeek 扩写，目标为3500—8000字。
- 扩写只使用原始资料、结构化草稿和 Tavily 搜索摘要，并要求按来源编号引用；扩写失败时仍保留原有草稿和短报告。

## [1.1.0-ai-parser-alpha.7] - 2026-07-28

### 新增

- 默认联网研究服务切换为 Tavily Basic Search，并继续由 DeepSeek 完成基础解析和研究报告整理。
- 每个案例最多执行2次基础搜索，每次最多返回10条结果；搜索优先中国地区内容，合并去重后最多保留20个可追溯来源。
- Tavily 免费方案每月1000搜索额度，按当前配置约可完成500个案例的联网检索；免费额度内搜索实际费用为0。

### 成本与容错

- 管理端搜索估价按超出免费额度后的 Tavily 按量价保守计算，约0.0576元/次；实际费用以服务商账单为准。
- 任一次搜索成功即可继续生成研究报告；全部失败、额度不足、鉴权失败或 DeepSeek 报告生成失败时保留基础解析草稿。
- 智谱、腾讯 WSA 与千问研究代码保留为可选备用，不再作为默认线上研究路径。

## [1.1.0-ai-parser-alpha.6] - 2026-07-28

### 新增

- 默认联网研究服务切换为智谱 Web Search API 的 `search_std` 国内搜索源。
- 每个案例自动执行最多2次搜索，分别覆盖项目身份与招投标、建设主体、功能架构、验收运营和成效证据；每次最多返回15条，合并后去重保留最多24个来源。
- 搜索费用按智谱 `search_std` 当前0.01元/次计入管理端估算，单个案例搜索费用上限约0.02元。

### 容错与边界

- 智谱部分查询失败时继续使用成功结果；全部失败、余额不足、鉴权失败或DeepSeek报告生成失败时保留基础解析草稿。
- 腾讯WSA与千问研究代码保留为可选备用，不再作为默认线上研究路径。

## [1.1.0-ai-parser-alpha.5] - 2026-07-28

### 新增

- 联网研究切换为腾讯 WSA 轻量版真实网页搜索，并由 DeepSeek 根据原始资料与搜索摘要生成结构化案例和长篇研究报告。
- 根据项目名称、地区、建设单位、招投标、架构与成效自动生成最多 6 组检索词，合并并去重最多 24 个可追溯来源。
- 扫描 PDF 由 Gemini 完成基础识别后，可以继续进入腾讯 WSA 与 DeepSeek 研究链路。
- 管理端费用估算计入腾讯 WSA 轻量版当前每次搜索约 0.018 元的调用成本。

### 容错与边界

- 腾讯 WSA 部分检索失败时保留成功结果；全部失败或 DeepSeek 报告生成失败时自动保留基础解析草稿。
- 联网摘要仅作为补充证据，公开资料不足时不得通过重复或编造内容强行凑字数。

## [1.1.0-ai-parser-alpha.4] - 2026-07-27

### 修复

- 扫描 PDF 改用 Gemini 官方 `generateContent` 文档解析接口，修复旧接口长时间等待后返回无法识别响应的问题。
- 兼容 Gemini `candidates` 与 `usageMetadata` 响应结构，正确读取结构化结果和 Token 用量。
- 增加 105 秒超时及上游异常分类，失败时提供可操作的中文提示。
- 兼容模型偶发返回 Markdown JSON 代码块的情况。

## [1.1.0-ai-parser-alpha.3] - 2026-07-27

### 新增

- 低成本基础解析默认使用 DeepSeek，联网研究使用千问内置搜索。
- 文本型 PDF 优先在浏览器本地提取文字，减少多模态文件调用费用。
- 千问联网失败时自动降级为 DeepSeek 基础解析，不再让整个解析任务失败。
- 管理端显示实际服务商、Token、搜索次数、估算费用和降级状态。

### 安全与边界

- DeepSeek、千问、Gemini 和 OpenAI 密钥只能保存在服务端环境变量中。
- 扫描 PDF 本地无法提取文字时，仍需要已配置的视觉模型。
- 估算费用依据公开单价和返回用量计算，只用于控制成本，不代替服务商最终账单。

## [1.1.0-ai-parser-alpha.2] - 2026-07-27

### 新增

- 案例解析支持 Gemini Google Search 联网研究，可自动补充官方公告、招投标、建设单位和媒体资料。
- 新增长篇案例研究、实际检索词和可验证来源链接，研究内容可在管理端复核并进入阅读器。
- 新增位置智能补全：只明确省份时，自动以省会城市中心作为地图展示锚点。
- 位置结果记录“原文明确、省会默认、城市中心推断”三种方法，并保留置信度与人工确认说明。

### 边界

- 联网研究会增加 Token 和 Google Search 调用量，由用户在管理端显式开启。
- 公开资料不足时不强行凑字数，未核验信息不得写成事实。
- 省会坐标只用于省级项目的地图展示，不代表项目实际落地点。

## [1.1.0-ai-parser-alpha.1] - 2026-07-27

### 新增

- 案例解析器支持 Gemini 与 OpenAI 双模型配置。
- 默认使用 `gemini-3.5-flash`，可通过环境变量切换回 OpenAI。
- 解析结果记录实际服务商、模型和 token 用量。
- 将余额不足、速率限制、Key无效和权限不足拆分为不同错误提示。

### 安全说明

- Gemini免费层只用于公开资料测试，不建议上传保密、个人或未公开项目资料。
- Gemini与OpenAI密钥均只存放在服务端环境变量中。

## [1.1.0-ai-parser-alpha.0] - 2026-07-27

### 新增

- 将管理端预设定时器替换为真实 OpenAI Responses API 调用。
- 支持粘贴网页正文、直接粘贴资料和最大 8MB 的 PDF。
- 使用严格 JSON Schema 约束案例字段，避免返回结构漂移。
- 展示关键字段证据、置信度、人工核验事项和本次 token 用量。
- AI 仅生成本地草稿，发布前必须人工确认来源、内容和地图坐标。

### 当前限制

- 网页链接只记录来源，不自动抓取正文。
- PDF 原文件不保存，未建立异步任务、独立 OCR、数据库和跨设备同步。
- 管理端尚未接入真实账号权限，仅适合受控测试。

## [1.0.0-prototype] - 2026-07-26

### 已完成

- 完成 DigitalX 城市数智应用案例库 V1 高保真交互原型。
- 建立公开首页、案例检索、组合筛选、城市地图与案例列表。
- 建立案例阅读器，支持滚动/分页、单双页、主题和字号偏好。
- 建立研读模式，展示结构化事实、建设内容、专业研判与证据来源。
- 建立管理工作台，演示资料导入、AI 解析、人工复核、位置确认和发布流程。
- 支持在当前浏览器保存草稿、待复核和已发布的本地案例。
- 按 Mintlify 设计纪律统一公开站、工作台与研读界面。
- 完成桌面与移动端响应式适配。
- 发布 Sites 线上演示版本。

### 工程化收口

- 重写项目 README。
- 补齐产品、功能边界、架构、设计、交接、V2 与 Git 协作文档。
- 增加 `.env.example` 与 AI 开发规则。
- 增加 Windows 可直接使用的开发和类型检查命令。
- 制作不含依赖、缓存、Git 历史和真实密钥的 VS Code 迁移包。

### 尚未实现

- 正式数据库、账号和管理员权限。
- 云端文件存储、跨设备同步。
- 真实 PDF/OCR/网页提取和大模型结构化。
- 草稿审核日志、版本历史、数据备份和恢复。
- 面向真实用户的生产级监控、安全与运维。
## [1.4.0-map-workbench-phase.2] - 2026-07-29

### 新增

- 地图工作台支持全国、省域、城市、项目四级地图导航。
- 全国层级保留城市聚合，其他层级切换为精确案例点位。
- 目录在案例选中后自动展开到对应分组并滚动定位。
- 搜索、筛选、目录、地图层级与当前案例同步到 URL，可刷新和分享恢复。
- 中央案例预览补充已复核图片、建设主体、项目阶段、核心建设、标签和坐标可信度。

### 边界

- 省级案例的省会坐标仍是展示锚点，不等同于项目实际落点。
- 只有已纳入且已人工复核的媒体资产进入中央预览。
- 本阶段继续在 `feature/v1.4-map-workbench` 开发，不替换 V1.3 正式站。
